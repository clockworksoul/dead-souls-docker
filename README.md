# Dead Souls: The Docker Image

To deploy, simply execute the `docker-compose` file:

`docker-compose up -d`

This will create a 'lib' directory in the working directory that contains all of your mudlib files.
 
